The following user has submitted a certificate linking request:

Name: ${name}
Username: ${username}
Certificate subject: ${subject}
Certificate issuer: ${issuer}

<#if notes?has_content>Notes:
${notes}</#if>
	
You can approve or reject this request by following the link below:

${indigoDashboardUrl}


The ${organisationName} management service